# IWDD Project 2026: Waste Disposal Detection
---

### Main Files (Root)

- **`test.py`** — **(MANDATORY DELIVERABLE)** The final script that receives the `--videos` and `--results` arguments and runs the full inference pipeline.
- **`test.ipynb`** — **(MANDATORY DELIVERABLE)** The Colab notebook that installs dependencies, loads the model, and executes `test.py`.
- **`requirements.txt`** — List of Python dependencies (e.g., `opencv-python`, `ultralytics`). Install with `pip install -r requirements.txt`.
- **`README.md`** — This file. The main documentation for the project.
- **`.gitignore`** — Specifies which files and folders Git should ignore.


