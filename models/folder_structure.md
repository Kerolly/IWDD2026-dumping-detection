# IWDD Project 2026: Waste Disposal Detection
---

### 📂 `models/`
**Purpose:** Contains the trained model weights.

- `yolov8n.pt` — The base YOLO model, pre-trained on COCO.
- `yolo_waste.pt` — *(Generated)* Our final fine-tuned model trained on `yolo_dataset/`. **This is the model to be delivered.**
